# 이 파일을 복사해서 .env.local 로 저장하고 실제 값을 채우세요. (.env.local 은 git에 올라가지 않습니다)

# 수파베이스 연결 정보 — 대시보드 → Settings → API 에서 확인
# anon 키는 공개되어도 되는 키입니다 (DB 접근은 RLS 정책이 통제)
VITE_SUPABASE_URL=https://xxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...
