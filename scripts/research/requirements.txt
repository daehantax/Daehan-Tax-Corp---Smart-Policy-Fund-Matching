# C-1 첨부파일 실태조사용 (조사 전용 — 운영 파이프라인 의존성 아님)
#
# 설치 (전역 파이썬을 건드리지 않도록 조사 데이터 폴더에 설치한다):
#   py -m pip install --target .research-data/pylibs -r scripts/research/requirements.txt
#
# 실행:
#   PYTHONPATH=.research-data/pylibs py scripts/research/extract_survey.py

olefile>=0.47      # HWP 5.0 (OLE 복합문서)
pdfplumber>=0.11   # PDF 텍스트 + 표 감지 + 스캔본 판별
openpyxl>=3.1      # XLSX
python-docx>=1.1   # DOCX
