# 대한세무법인 — 맞춤형 정책자금 스마트 매칭

## 작업 시작 전 필독

**[docs/WORK_PLAN.md](docs/WORK_PLAN.md)를 먼저 읽고 시작한다.**
지금 진행 중인 고도화 작업의 단계·상태·실측 기록이 모두 그 문서에 있다.
작업이 끝나면 해당 항목의 체크박스와 「작업 로그」를 갱신한다.

이미 확인된 사실은 `WORK_PLAN.md` §7 실측 기록에 있다. **다시 조사하지 않는다.**

## 이 저장소가 하는 일

고객사가 사업자등록번호 + 대표자 성명을 입력하면, 회사의 지역·시군·업종에 맞는
정부지원 공고를 골라 보여준다. 공고는 기업마당 API에서 매일 새벽 자동 동기화한다.

```
services/geo.ts              행정구역 판정 (시도·시군구·권역) — 지역 로직의 단일 출처
services/matchingService.ts  매칭 점수·자격 판정
services/csvService.ts       공고 로드(수파베이스 → 구글시트 → CSV 폴백) + 스마트 태깅
services/mockDb.ts           고객사 확인 (Edge Function 호출. 폴백 없음)
scripts/sync-bizinfo.mjs     기업마당 API → CSV + 수파베이스 업서트 (GitHub Actions)
supabase/functions/verify-brn 사업자번호 + 대표자명 대조 Edge Function
```

## 깨뜨리면 안 되는 것

- **확실하지 않으면 제외하지 않는다.** 자격 있는 공고를 잘못 지우는 쪽이 훨씬 나쁘다.
  제외(`score 0`)는 지역·시·군처럼 확정 가능한 축에서만. 나머지는 감점 + 주의 표시.
- **`geo.ts`가 지역 판정의 단일 출처다.** 세 번째 복제본을 만들지 않는다.
  (`sync-bizinfo.mjs`가 이미 같은 규칙을 복제하고 있어 `syncParity` 테스트로 묶어둔 상태 —
  한쪽만 고치면 DB 값과 화면 계산이 조용히 어긋난다. **반드시 함께 수정한다.**)
- **`clients` 테이블은 RLS 전잠금 유지.** 컬럼·정책 수정 금지. 접근은 Edge Function 경유만.
- **고객사 데이터를 외부 AI 요청에 넣지 않는다.** AI에는 공개 데이터(공고문)만 보낸다.
- **`npm test`는 배포 게이트다.** 통과하지 않으면 배포되지 않아야 한다.
- **동기화 실패 시 기존 데이터를 덮어쓰지 않는다.** 0건이면 종료코드 1로 끝낸다.

## 테스트

```bash
npm test
```

케이스는 **실제로 틀렸던 지점**에서 가져왔다. 실패하면 그 버그가 돌아온 것이다.
새 규칙을 만들 때도 같은 방식으로 — 실측 근거를 주석에 건수까지 남긴다.

## 커밋

- 커밋·푸시는 요청받았을 때만 한다.
- `main`은 푸시 시 자동 배포된다(`deploy-pages.yml`). 작업은 브랜치에서 한다.
- 데이터 자동 동기화 커밋(`chore: 기업마당 공고 데이터 자동 동기화`)은 봇이 남기는 것이다.
